{
  "name": "jua-services",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "type-check": "bun run --filter backend type-check && bun run --filter frontend type-check"
  }
}